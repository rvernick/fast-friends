import { StyleSheet, View, Text } from "react-native";

const Settings = () => {
  return (
    <View>
      <Text>Bikes</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default Settings;
