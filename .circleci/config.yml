# This config was automatically generated from your source code
# Stacks detected: cicd:gitlab-workflows:.,deps:node:frontend,test:jest:
version: 2.1
orbs:
  node: circleci/node@5
jobs:
  start-job:
  steps:
    - checkout:
      path: ~/
  test-backend:
   executor: node/default
   working_directory: ~/backend
   steps:

    - node/install-packages:
        pkg-manager: npm
    - run:
        name: Install
        command: cd ~/backend; npm install
    - run:
        name: Test
        command: cd ~/backend; npm run test
  test-frontend:
    # Install node dependencies and run tests
    executor: node/default
    working_directory: ~/frontend
    environment:
      JEST_JUNIT_OUTPUT_DIR: ./test-results/
    steps:
      - node/install-packages:
          pkg-manager: npm
      - run:
          command: ~/frontend; npm install
      - run:
          name: Run tests
          command: ~/frontend; npm run test --ci --runInBand --reporters=default --reporters=jest-junit
      - store_test_results:
          path: ./test-results/
  deploy:
    # This is an example deploy job, not actually used by the workflow
    docker:
      - image: cimg/base:stable
    steps:
      # Replace this with steps to deploy to users
      - run:
          name: deploy
          command: '#e.g. ./deploy.sh'
      - run:
          name: found gitlab workflows config
          command: ':'
workflows:
  build-and-test:
    jobs:
      - start-job
      - test-backend
      - test-frontend
    # - deploy:
    #     requires:
    #       - test-node
